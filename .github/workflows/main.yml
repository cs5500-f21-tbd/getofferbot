name: Fly Deploy
on: [push]
env:
  FLY_API_TOKEN: ${{ secrets.N8yCQ0M3tBXgv9j5q2s7naFCNrgnaST-iXjuhNA4U0Y}}
jobs:
  deploy:
      name: Deploy app
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: superfly/flyctl-actions@1.1
          with:
            args: "deploy"
